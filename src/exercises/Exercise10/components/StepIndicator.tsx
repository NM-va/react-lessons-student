
interface Props {
  totalSteps: number;
  currentStep: number;
}
export const StepIndicator (props: Props) => {
const {totalSteps, currentStep} = props;

  for (let i = 0; i < totalSteps; i++) {

  }

  return (
    <>
    </>
  )
}